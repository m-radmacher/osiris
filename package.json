{
  "name": "osiris",
  "description": "A simple way to extract licenses from NuGet packages",
  "license": "MIT",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "lint": "biome ci src/",
    "lint:fix": "biome lint --write src/",
    "build": "tsc",
    "package": "esbuild src/index.ts --bundle --platform=node --target=node24 --format=cjs --minify --outfile=dist/index.js --loader:.json=json",
    "clean": "rimraf dist/ lib/",
    "release": "pnpm run clean && pnpm run package",
    "dev": "tsx watch src/index.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.13",
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@actions/artifact": "^6.2.1",
    "@actions/core": "^3.0.1",
    "axios": "^1.2.0",
    "chalk": "^5.1.2",
    "extract-zip": "^2.0.1",
    "fast-xml-parser": "^4.0.12"
  },
  "packageManager": "pnpm@11.0.0"
}
